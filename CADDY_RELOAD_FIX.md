# 🔧 Исправление зависания Caddy reload

## Проблема

Команда `sudo systemctl reload caddy` зависла (залагала).

## Решение

### Вариант 1: Прервать и перезапустить

1. Нажмите `Ctrl+C` чтобы прервать зависшую команду

2. Проверьте статус Caddy:
```bash
sudo systemctl status caddy
```

3. Используйте `restart` вместо `reload`:
```bash
sudo systemctl restart caddy
```

4. Проверьте статус:
```bash
sudo systemctl status caddy
```

### Вариант 2: Если restart тоже зависает

1. Остановите Caddy принудительно:
```bash
sudo systemctl stop caddy
```

2. Подождите несколько секунд

3. Запустите снова:
```bash
sudo systemctl start caddy
```

4. Проверьте статус:
```bash
sudo systemctl status caddy
```

### Вариант 3: Если ничего не помогает

1. Убейте процесс Caddy:
```bash
sudo pkill -9 caddy
```

2. Подождите 2-3 секунды

3. Запустите Caddy:
```bash
sudo systemctl start caddy
```

4. Проверьте статус:
```bash
sudo systemctl status caddy
```

## Проверка работы после перезапуска

```bash
# Проверьте логи
sudo journalctl -u caddy -n 50

# Проверьте, что Caddy слушает порты
sudo ss -tulpn | grep caddy

# Проверьте доступность сайта
curl -I https://glco.us
```

## Форматирование Caddyfile (опционально)

Caddy предупреждает, что файл не отформатирован. Можно отформатировать:

```bash
sudo caddy fmt --overwrite /etc/caddy/Caddyfile
```

Но это не обязательно - файл работает и так.
