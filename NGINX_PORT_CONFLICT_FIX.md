# 🔧 Исправление ошибки: порт 80 занят

## Проблема

Ошибка `bind() to 0.0.0.0:80 failed (98: Unknown error)` означает, что порт 80 уже занят другим процессом.

## Решение

### Шаг 1: Найдите процесс, который занимает порт 80

```bash
# Вариант 1: Используя netstat
sudo netstat -tulpn | grep :80

# Вариант 2: Используя lsof
sudo lsof -i :80

# Вариант 3: Используя ss
sudo ss -tulpn | grep :80

# Вариант 4: Используя fuser
sudo fuser 80/tcp
```

### Шаг 2: Определите процесс

Обычно это может быть:
- **Apache** (если установлен)
- **Другой Nginx процесс** (если уже запущен)
- **Другой веб-сервер**

### Шаг 3: Решение в зависимости от процесса

#### Если это Apache:

```bash
# Остановите Apache
sudo systemctl stop apache2

# Отключите автозапуск Apache (если не нужен)
sudo systemctl disable apache2

# Запустите Nginx
sudo systemctl start nginx
```

#### Если это другой Nginx процесс:

```bash
# Найдите все процессы Nginx
ps aux | grep nginx

# Остановите все процессы Nginx
sudo pkill nginx

# Или более безопасно:
sudo systemctl stop nginx
sudo pkill -9 nginx

# Запустите Nginx снова
sudo systemctl start nginx
```

#### Если это другой процесс:

```bash
# Узнайте PID процесса из вывода netstat/lsof
# Например, если PID = 1234:

# Посмотрите, что это за процесс
ps aux | grep 1234

# Остановите процесс (замените 1234 на реальный PID)
sudo kill 1234

# Или принудительно
sudo kill -9 1234
```

### Шаг 4: Проверьте, что порт 80 свободен

```bash
sudo netstat -tulpn | grep :80
# Должно быть пусто (или только Nginx после запуска)
```

### Шаг 5: Запустите Nginx

```bash
sudo systemctl start nginx
sudo systemctl status nginx
```

## Альтернативное решение: Использовать другой порт (не рекомендуется)

Если вы не можете освободить порт 80, можно настроить Nginx на другой порт (например, 8080), но это потребует изменения конфигурации и не рекомендуется для production.

## Проверка после исправления

```bash
# 1. Проверьте статус Nginx
sudo systemctl status nginx

# 2. Проверьте, что Nginx слушает порт 80
sudo netstat -tulpn | grep nginx

# 3. Проверьте конфигурацию
sudo nginx -t

# 4. Перезагрузите Nginx
sudo systemctl reload nginx
```

## Частые случаи

### Случай 1: Apache и Nginx установлены вместе

Обычно на сервере может быть установлен Apache. Остановите Apache:

```bash
sudo systemctl stop apache2
sudo systemctl disable apache2
sudo systemctl start nginx
```

### Случай 2: Nginx уже запущен, но не виден в systemctl

```bash
# Найдите процессы Nginx
ps aux | grep nginx

# Убейте все процессы
sudo pkill -9 nginx

# Запустите через systemctl
sudo systemctl start nginx
```

### Случай 3: Другой веб-сервер (например, Caddy, Traefik)

```bash
# Найдите процесс
sudo lsof -i :80

# Остановите его
sudo systemctl stop <service-name>
```
