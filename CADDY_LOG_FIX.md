# 🔧 Исправление ошибки лог-файла Caddy

## Проблема

Ошибка: `opening log writer using &logging.FileWriter{Filename:"/var/log/caddy/webhook-access.log"`

Caddy не может открыть файл лога из-за отсутствия директории или прав доступа.

## Решение

### Вариант 1: Создать директорию и установить права

```bash
# 1. Создайте директорию для логов
sudo mkdir -p /var/log/caddy

# 2. Установите правильные права (Caddy обычно работает от пользователя caddy)
sudo chown -R caddy:caddy /var/log/caddy
sudo chmod 755 /var/log/caddy

# 3. Попробуйте запустить Caddy снова
sudo systemctl start caddy

# 4. Проверьте статус
sudo systemctl status caddy
```

### Вариант 2: Временно убрать логирование

Если не нужны логи, можно временно убрать блок `log` из конфигурации:

1. Откройте Caddyfile:
```bash
sudo nano /etc/caddy/Caddyfile
```

2. Найдите блок `webhook.glco.us` и закомментируйте или удалите блок `log`:

```caddy
webhook.glco.us {
    reverse_proxy 127.0.0.1:4010
    # log {
    #     output file /var/log/caddy/webhook-access.log
    #     format json
    # }
}
```

3. Сохраните и перезапустите:
```bash
sudo systemctl start caddy
```

### Вариант 3: Использовать стандартный лог

Замените кастомный лог на стандартный:

```caddy
webhook.glco.us {
    reverse_proxy 127.0.0.1:4010
    # Используем стандартное логирование вместо кастомного файла
}
```

## Проверка после исправления

```bash
# Проверьте статус
sudo systemctl status caddy

# Проверьте логи systemd
sudo journalctl -u caddy -n 50

# Проверьте, что Caddy запущен
sudo systemctl is-active caddy
```

## Если проблема с правами сохраняется

Проверьте пользователя, от которого работает Caddy:

```bash
# Проверьте конфигурацию сервиса
sudo systemctl cat caddy.service | grep User

# Или проверьте запущенный процесс
ps aux | grep caddy
```

Убедитесь, что пользователь `caddy` (или другой пользователь из конфигурации) имеет права на запись в `/var/log/caddy`.
