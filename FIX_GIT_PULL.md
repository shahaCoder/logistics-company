# 🔧 Исправление ошибки git pull

## Проблема

Ошибка: `Your local changes to the following files would be overwritten by merge: public/sitemap-0.xml`

Файл `sitemap-0.xml` генерируется автоматически при сборке Next.js и не должен быть в git.

## Решение

### Вариант 1: Удалить файл и сделать pull (быстрое решение)

```bash
cd /var/www/backend/backend

# Удалите файл
rm public/sitemap-0.xml
rm public/sitemap.xml  # если есть

# Сделайте pull
git pull
```

### Вариант 2: Stash изменений

```bash
cd /var/www/backend/backend

# Сохраните изменения во временное хранилище
git stash

# Сделайте pull
git pull

# Если нужно, верните изменения (обычно не нужно для sitemap)
# git stash pop
```

### Вариант 3: Добавить в .gitignore (правильное решение)

```bash
cd /var/www/backend/backend

# Добавьте sitemap файлы в .gitignore
echo "public/sitemap*.xml" >> .gitignore
echo "sitemap*.xml" >> .gitignore

# Удалите файл из git (но оставьте локально)
git rm --cached public/sitemap-0.xml 2>/dev/null || true
git rm --cached public/sitemap.xml 2>/dev/null || true

# Сделайте pull
git pull
```

## Рекомендуемое решение

Лучше всего использовать Вариант 1 (просто удалить файл), так как он генерируется автоматически при каждой сборке.

```bash
cd /var/www/backend/backend
rm -f public/sitemap*.xml
git pull
```

После pull файлы снова сгенерируются при следующей сборке.
