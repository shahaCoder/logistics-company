# 🔧 Принудительное исправление git pull

## Проблема

Файл `public/sitemap-0.xml` отслеживается git и имеет локальные изменения, которые блокируют pull.

## Решение

### Вариант 1: Удалить из git и сделать pull

```bash
cd /var/www/backend/backend

# Удалите файл из git индекса
git rm --cached public/sitemap-0.xml

# Или если файл уже удален локально, но есть в git
git rm public/sitemap-0.xml

# Добавьте в .gitignore
echo "public/sitemap*.xml" >> .gitignore
echo "sitemap*.xml" >> .gitignore

# Сделайте pull
git pull
```

### Вариант 2: Stash изменений

```bash
cd /var/www/backend/backend

# Сохраните все изменения во временное хранилище
git stash

# Сделайте pull
git pull

# Файлы sitemap сгенерируются при следующей сборке
```

### Вариант 3: Принудительный reset (если не важно сохранить изменения)

```bash
cd /var/www/backend/backend

# Отмените все локальные изменения в этом файле
git checkout -- public/sitemap-0.xml

# Или удалите файл полностью
git rm public/sitemap-0.xml

# Сделайте pull
git pull
```

### Вариант 4: Полный reset (крайний случай)

```bash
cd /var/www/backend/backend

# Отмените все локальные изменения
git reset --hard HEAD

# Сделайте pull
git pull
```

## Рекомендуемое решение

Используйте Вариант 1 или Вариант 2 (stash) - они безопаснее.
