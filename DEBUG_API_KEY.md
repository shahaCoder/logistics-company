# 🔍 Отладка: Invalid API key

## Проверка переменных окружения

### 1. Проверьте, что .env.local создан и содержит правильные значения

```bash
cd /var/www/backend
cat .env.local
```

Должно быть:
```bash
NEXT_PUBLIC_BOT_API_BASE=https://webhook.glco.us/api
NEXT_PUBLIC_BOT_API_KEY=aB3cD4eF5gH6iJ7kL8mN9oP0qR1sT2uV3wX4yZ5aB6cD7eF8gH9iJ0kL1mN2oP3
```

### 2. Убедитесь, что проект пересобран ПОСЛЕ создания .env.local

```bash
cd /var/www/backend
npm run build
```

### 3. Проверьте в браузере, что переменные доступны

Откройте DevTools (F12) → Console и выполните:
```javascript
console.log('API_BASE:', process.env.NEXT_PUBLIC_BOT_API_BASE)
console.log('API_KEY:', process.env.NEXT_PUBLIC_BOT_API_KEY)
```

Если выводит `undefined` - переменные не встроились, нужно пересобрать.

### 4. Проверьте Network tab

1. Откройте DevTools (F12) → Network
2. Обновите страницу
3. Найдите запрос к `/api/oil-change/list`
4. Проверьте Request Headers - должен быть заголовок `X-API-Key`

Если заголовка нет или он пустой - проблема в коде или переменных.

### 5. Проверьте, что API ключ правильный

```bash
# Проверьте на сервере бота
cat /opt/pti-bot/.env | grep ADMIN_API_KEY
```

Должен совпадать с `NEXT_PUBLIC_BOT_API_KEY`.

### 6. Проверьте напрямую через curl

```bash
curl -X GET "https://webhook.glco.us/api/oil-change/list" \
  -H "X-API-Key: aB3cD4eF5gH6iJ7kL8mN9oP0qR1sT2uV3wX4yZ5aB6cD7eF8gH9iJ0kL1mN2oP3"
```

Если работает - проблема в Next.js, если нет - проблема в API ключе.

## Возможные проблемы

1. **Переменные не встроились** - нужно пересобрать после создания .env.local
2. **Неправильный формат .env.local** - не должно быть пробелов вокруг `=`
3. **Кэш браузера** - очистите кэш или откройте в режиме инкогнито
4. **API ключ не совпадает** - проверьте на сервере бота
