# 🔧 Интеграция Oil Change API (pti-bot)

## Обзор

В админ панель интегрирован REST API из проекта pti-bot для управления oil change статусом траков через веб-интерфейс.

## Что было сделано

### 1. API функции (`src/utils/oilChangeApi.ts`)
- `getOilChangeList()` - получение списка всех траков с их oil change статусом
- `getOilChangeStatus(truckName)` - получение статуса конкретного трака
- `resetOilChange(truckName, mileage?)` - сброс oil change для трака
- `getTrucksList()` - получение списка всех траков (для выпадающего списка)

### 2. UI компонент (`src/app/internal-driver-portal-7v92nx/oil-change/page.tsx`)
- Таблица со всеми траками и их статусом
- Фильтры: All, Warning, Overdue
- Сортировка: по оставшимся милям, статусу, имени
- Кнопка "Reset Oil Change" с модальным окном
- Автообновление данных (каждые 5 минут, опционально)
- Summary карточки с общей статистикой

### 3. Навигация
- Добавлен пункт меню "Oil Change" в админ панели

## Настройка

### Шаг 1: Настройка Nginx Reverse Proxy

Добавьте в конфигурацию Nginx на сервере glco.us:

```nginx
# В /etc/nginx/sites-available/glco.us (или аналогичный файл)
server {
    server_name glco.us www.glco.us;
    
    # ... существующие location блоки для основного сайта ...
    
    # API для бота (reverse proxy)
    location /api/bot/ {
        proxy_pass http://pti-bot-server-ip:4010/api/;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        
        # CORS headers (если нужно дополнительно)
        add_header 'Access-Control-Allow-Origin' 'https://glco.us' always;
        add_header 'Access-Control-Allow-Methods' 'GET, POST, OPTIONS' always;
        add_header 'Access-Control-Allow-Headers' 'X-API-Key, Content-Type' always;
        
        # Handle OPTIONS preflight
        if ($request_method = 'OPTIONS') {
            return 204;
        }
    }
}
```

**Важно:**
- Замените `pti-bot-server-ip` на реальный IP адрес сервера, где работает pti-bot
- Если бот и админка на одном сервере, используйте `http://localhost:4010/api/`
- После изменений: `sudo nginx -t` и `sudo systemctl reload nginx`

### Шаг 2: Настройка переменных окружения

Добавьте в `.env` файл проекта (или `.env.local` для Next.js):

```bash
# Base URL для API бота (после настройки Nginx)
NEXT_PUBLIC_BOT_API_BASE=https://glco.us/api/bot

# API ключ для аутентификации (должен совпадать с ADMIN_API_KEY на сервере бота)
NEXT_PUBLIC_BOT_API_KEY=your-secret-api-key-here
```

**Важно:**
- Используйте тот же ключ, который установлен в `ADMIN_API_KEY` на сервере pti-bot
- Для production используйте переменные окружения на сервере (не коммитьте в git!)

### Шаг 3: Перезапуск приложения

После настройки переменных окружения:

```bash
# Если используете PM2
pm2 restart nextjs-app --update-env

# Или если используете другой процесс менеджер
# Перезапустите Next.js приложение
```

## API Endpoints

После настройки Nginx, API будет доступно по адресам:

- `GET https://glco.us/api/bot/oil-change/list` - список всех траков
- `GET https://glco.us/api/bot/oil-change/:truckName` - статус конкретного трака
- `POST https://glco.us/api/bot/oil-change/reset` - сброс oil change
- `GET https://glco.us/api/bot/trucks` - список всех траков

Все запросы требуют заголовок `X-API-Key` с секретным ключом.

## Тестирование

После настройки Nginx, протестируйте endpoints:

```bash
# Получить список всех траков
curl -X GET "https://glco.us/api/bot/oil-change/list" \
  -H "X-API-Key: your-api-key"

# Получить статус трака
curl -X GET "https://glco.us/api/bot/oil-change/Truck%20717" \
  -H "X-API-Key: your-api-key"

# Reset oil change
curl -X POST "https://glco.us/api/bot/oil-change/reset" \
  -H "X-API-Key: your-api-key" \
  -H "Content-Type: application/json" \
  -d '{"truckName": "Truck 717", "mileage": 450000}'
```

## Использование в админ панели

1. Войдите в админ панель: `https://glco.us/internal-driver-portal-7v92nx/`
2. Перейдите в раздел "Oil Change" в меню
3. Просмотрите список всех траков с их статусом
4. Используйте фильтры для поиска траков с предупреждениями
5. Нажмите "Reset" для сброса oil change для конкретного трака

## Структура данных

### OilChangeTruck
```typescript
{
  vehicleName: string;
  currentMileage: number | null;
  lastOilChangeMileage: number;
  lastOilChangeDate: string;
  nextDueMileage: number;
  remainingMiles: number | null;
  status: 'OK' | 'WARNING' | 'OVERDUE';
}
```

### OilChangeListResponse
```typescript
{
  success: boolean;
  data: OilChangeTruck[];
  summary: {
    total: number;
    ok: number;
    warning: number;
    overdue: number;
  };
}
```

## Обработка ошибок

- Если API недоступен, показывается понятное сообщение об ошибке
- Ошибки логируются в консоль браузера для отладки
- При ошибках аутентификации (401) пользователь перенаправляется на страницу входа

## Безопасность

- API key хранится в переменных окружения (не в коде)
- Все запросы идут через HTTPS
- API key проверяется на каждом запросе на сервере бота
- CORS настроен только для домена glco.us

## Troubleshooting

### API не отвечает
1. Проверьте, что Nginx правильно настроен: `sudo nginx -t`
2. Проверьте, что бот запущен на сервере: `pm2 logs pti-bot`
3. Проверьте доступность API напрямую: `curl http://pti-bot-server:4010/api/oil-change/list -H "X-API-Key: ..."`

### Ошибка 401 (Unauthorized)
1. Проверьте, что `NEXT_PUBLIC_BOT_API_KEY` установлен правильно
2. Убедитесь, что ключ совпадает с `ADMIN_API_KEY` на сервере бота

### CORS ошибки
1. Проверьте настройки CORS в Nginx конфигурации
2. Убедитесь, что `ADMIN_PANEL_URL` в .env бота указывает на `https://glco.us`

## Контакты

Если нужны уточнения по API или структуре данных:
- Проверьте логи бота: `pm2 logs pti-bot`
- Endpoints доступны по адресу: `http://pti-bot-server:4010/api/oil-change/*`
