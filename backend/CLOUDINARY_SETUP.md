# 🔧 Настройка Cloudinary

## Проблема

Если вы видите ошибку:
```
Error: Must supply api_key
```

Это означает, что переменные окружения Cloudinary не настроены.

## Решение

### 1. Получите учетные данные Cloudinary

1. Зарегистрируйтесь на [cloudinary.com](https://cloudinary.com)
2. Перейдите в Dashboard
3. Найдите следующие значения:
   - **Cloud Name**
   - **API Key**
   - **API Secret**

### 2. Добавьте переменные в `.env` файл

Создайте или отредактируйте файл `.env` в папке `backend/`:

```env
CLOUDINARY_CLOUD_NAME=your_cloud_name
CLOUDINARY_API_KEY=your_api_key
CLOUDINARY_API_SECRET=your_api_secret
```

### 3. Перезапустите сервер

После добавления переменных окружения перезапустите backend сервер:

```bash
npm run dev
# или
npm start
```

### 4. Проверка

При запуске сервера вы должны увидеть:
```
✅ All required environment variables are set
```

Если видите:
```
❌ Missing required environment variables:
   - CLOUDINARY_CLOUD_NAME
   - CLOUDINARY_API_KEY
   - CLOUDINARY_API_SECRET
```

Значит переменные не загружены. Проверьте:
- Файл `.env` находится в папке `backend/`
- Переменные написаны без пробелов и кавычек
- После изменений `.env` сервер перезапущен

## Что было исправлено

1. ✅ **Валидация при старте** - сервер проверяет наличие всех переменных
2. ✅ **Ленивая инициализация** - Cloudinary инициализируется только при использовании
3. ✅ **Понятные ошибки** - вместо "Must supply api_key" теперь показывается, какие переменные отсутствуют
4. ✅ **Улучшенная обработка ошибок** - более информативные сообщения для пользователей

## Production

В production убедитесь, что переменные окружения установлены на сервере:

```bash
# Пример для systemd
Environment="CLOUDINARY_CLOUD_NAME=your_cloud_name"
Environment="CLOUDINARY_API_KEY=your_api_key"
Environment="CLOUDINARY_API_SECRET=your_api_secret"
```

Или используйте переменные окружения вашего хостинга (Heroku, Railway, etc.)
